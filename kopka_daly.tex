% !TEX TS-program = pdflatex
% !TEX encoding = UTF-8 Unicode

% use larger type; default would be 10pt
\documentclass[11pt,twoside]{article} 
% set input encoding (not needed with XeLaTeX)
\usepackage[utf8]{inputenc}  
 
% the remaining packages are optional.
%%% PAGE DIMENSIONS
\usepackage[letterpaper,left=1.25in,right=1.25in,top=1.25in,bottom=1in]{geometry} 
% read geometry.pdf for detailed page layout information

%%% HEADERS & FOOTERS
\usepackage{fancyhdr} % This should be set AFTER setting up the page geometry
\pagestyle{fancy}     % options: empty , plain , fancy
% turn on centered page numbers only
\renewcommand{\headrulewidth}{0pt} % customize the layout...
\lhead{}\chead{}\rhead{}
\lfoot{}\cfoot{\thepage}\rfoot{}
% defeats headheight warning
\setlength{\headheight}{14pt}

%%% SECTION TITLE APPEARANCE
\usepackage{sectsty}
\allsectionsfont{\sffamily\mdseries\upshape} % (See the fntguide.pdf for font help)

%%% FONT MANAGEMENT
\usepackage[T1]{fontenc}
% use Courier for monospace
\usepackage{courier}
% use Bitstream Charter for Roman
\usepackage{charter}
% use Avant Garde for sans-serif
\usepackage{avant}

%%% MISC. PACKAGES
\usepackage{booktabs} % for better looking tables
\usepackage{alltt}    % allow monospace font sections that recognize commands
\usepackage{verbatim} % allows nicer verbatim text
\usepackage{etoolbox} 
% take the padding out of alltt and verbatim sections
\AtBeginEnvironment{alltt}{\setlength{\topsep}{0pt}} 
\AtBeginEnvironment{verbatim}{\setlength{\topsep}{0pt}}
\usepackage{longtable} % for tables that span pages
\usepackage{paralist}  % flexible & customizable lists (e.g. enumerate/itemize, etc.)
\usepackage{parskip}   % flush left paragraphs
\usepackage{subfig}    % make it possible to include more than one captioned figure/table in a single float
\usepackage{textcomp}  % access to truly straight quotes using \textquotesingle
\usepackage[pdftex]{graphicx} % support the \includegraphics command and options
% may need to restore color package in the preceding line
% widow and orphan management
\usepackage[all]{nowidow}
% hyperlink appearance control
\usepackage[colorlinks=true,hyperfootnotes=false,linkcolor=blue]{hyperref}

% table inter-column spacing
\setlength{\tabcolsep}{12pt}

%%% some custom commands
% shorthand for true single quotes
\newcommand{\q}{\textquotesingle}
% shorthand for indenting 
\newcommand{\ind}{\hspace{2em}}

% \usepackage{microtype}  % this helps avoid overfull hbox warnings
% add some hbox badness tolerance
% \hfuzz=2pt
% show overfull hboxes
% \overfullrule=1.25in

%%% END Article customizations

% beginning of document content

\title{%
	{\bfseries A \LaTeX\ Primer} \\[1ex]
    \small Notes from {\itshape Guide to \LaTeX}}
\author{Dan Liddell}
\date{} % Activate to display a given date or no date (if empty),
        % otherwise the current date is printed 

\begin{document}

\maketitle
\thispagestyle{empty}
\input{Inputs/foundational_ideas}
\input{Inputs/space_characters}
\input{Inputs/commands_decl_env}
\input{Inputs/sectioning}
\input{Inputs/whitespace_pagination}
\input{Inputs/fonts}
\input{Inputs/numbering}
\input{Inputs/tables}
\input{Inputs/graphics_color}
\input{Inputs/drawing}
\input{Inputs/cross_references}
\input{Inputs/conditional_text}

\end{document}
